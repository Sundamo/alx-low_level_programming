#include "main.h"

/**
 * _abs - Check code
 * @r: An integre input
 * Description: This function returns absolute value of a number
 * Return: 0
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (r * -1);
	}
}
